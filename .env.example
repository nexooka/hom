# OPTIONAL but recommended — gets you img2img (model sees real reference images)
# Free token at huggingface.co → Settings → Access Tokens → New token (Read)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Without HF_TOKEN the app still works via Pollinations.ai (no key needed)
